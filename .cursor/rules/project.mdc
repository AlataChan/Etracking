---
description: 
globs: 
alwaysApply: true
---
# Project Rules for Order PDF Secure Downloader

## always

- 所有下载文件命名需与订单号一致，避免人工无法识别。
- 默认使用 Playwright 非 headless 模式，防止反爬虫识别。
- 模块间要解耦，数据读取、页面跳转、PDF生成分别在不同模块完成。
- 日志统一使用 loguru 记录成功与失败信息。
- 所有敏感信息（如账号密码）必须写入 config/settings.yaml，不可硬编码。

---

# 项目智能规则

## 编码规范

1. 使用Python 3.9+语法
2. 使用JSDoc风格注释
3. 遵循PEP 8代码规范
4. 使用类型注解提高代码可读性
5. 模块化设计，单一职责原则
6. 异常处理必须明确具体异常类型

## 命名约定

1. 文件名使用snake_case (例如: excel_reader.py)
2. 类名使用PascalCase (例如: SessionManager)
3. 方法和变量使用snake_case (例如: get_order_number)
4. 常量使用UPPER_SNAKE_CASE (例如: DEFAULT_TIMEOUT)
5. 私有方法和变量以下划线开头 (例如: _internal_method)

## 架构模式

1. 使用模块化结构，每个模块负责单一功能域
2. 遵循任务管道式执行流程
3. 配置与代码分离，使用YAML存储配置
4. 错误处理采用"重试+记录"策略
5. 使用装饰器实现人类行为模拟
6. 日志分级：INFO(成功操作)，WARNING(需注意)，ERROR(失败)

## 用户偏好

1. 文档使用中文编写
2. 日志信息使用中文输出
3. 程序运行时显示进度条
4. 配置文件支持覆盖默认设置
5. 错误信息应具体明确，便于排查
6. PDF命名格式为"提单号.pdf"

## 安全实践

1. 不在代码中硬编码凭证信息
2. 会话状态持久化到独立文件
3. 请求间隔随机化，模拟人类行为
4. 避免频繁登录，优先使用已保存会话
5. 敏感信息在日志中脱敏处理
6. 异常处理不暴露系统详细信息

## 技术决策记录

1. 使用Playwright而非Selenium - 更现代的API，更好的等待机制
2. 使用非Headless模式 - 降低被检测为自动化工具的风险
3. 使用Pandas处理Excel - 支持多Sheet读取和数据处理
4. 使用Loguru而非标准logging - 更简洁的API和更好的格式化
5. PDF直接导出而非HTML转换 - 更简单可靠的方法
6. 使用YAML配置而非JSON - 更易读且支持注释

## 常见问题解决方案

1. 会话失效：检测登录状态，失效时自动重新登录
2. 网络超时：实现指数退避重试策略
3. 选择器失效：使用多重选择策略，优先ID和data属性
4. PDF导出失败：尝试不同的页面格式和等待策略
5. Excel读取错误：提供明确的格式要求和错误提示
6. 账号限制：实现请求频率控制和会话管理

## 开发工作流

1. 先实现核心功能，再添加错误处理和优化
2. 每个模块开发后进行独立测试
3. 主要功能变更记录在activeContext.md
4. 问题和解决方案记录在progress.md
5. 配置变更需同步更新文档
6. 重要技术决策记录在.cursorrules 
