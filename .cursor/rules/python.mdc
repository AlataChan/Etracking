---
description: 
globs: *.py
alwaysApply: false
---

## 🐍 Python Style Rules

### ✅ General Python Rules

- 所有变量、函数、文件名使用 `snake_case`。
- 类名使用 `PascalCase`。
- 每个模块顶部需加 `docstring`，说明模块职责。
- 函数必须使用类型注解，例如：`def process(order_id: str) -> bool:`
- 所有模块需通过 `__init__.py` 显式声明包结构。

---

### 📄 Docstring Style

- 使用 **Google 风格** 文档注释，例如：

```python
def fetch_pdf(order_id: str) -> bool:
    """下载并保存给定订单号的 PDF 文件。

    Args:
        order_id (str): 订单编号

    Returns:
        bool: 下载成功返回 True，否则 False
    """
```

---

### 🧪 Exception & Logging

- 所有关键流程必须用 `try/except` 包裹，避免脚本中断。
- 错误日志需记录完整异常栈：
  ```python
  try:
      ...
  except Exception as e:
      logger.exception(f"处理订单 {order_id} 出错: {e}")
  ```

- 所有日志使用 `loguru` 实现，禁止使用 `print()`。

---

### 📦 File Structure Conventions

- 每个模块职责单一、结构分层清晰：

| 文件名 | 职责 |
|--------|------|
| `main.py` | 主控流程协调、任务分发 |
| `session_manager.py` | 登录与会话持久化管理 |
| `receipt_printer.py` | 弹窗监听与 PDF 导出 |
| `humanizer.py` | 模拟人类行为操作工具 |
| `excel_reader.py` | Excel数据提取工具 |
| `utils.py` | 公共工具函数（如路径、时间、日志） |

---

### 🔁 Function Rules

- 每个函数最长 **不超过 40 行**。
- 函数内尽量少用嵌套 `if/else`，可用 early return 优化。
- 函数应聚焦单一职责，不建议一个函数做两件事。

---

### ✅ Type Hints & Lint

- 所有函数参数、返回值都要加上类型注解。
- 项目使用 `ruff` 或 `flake8` 进行风格检查。
- 格式化建议使用 `black`。

---

### 📄 示例标准函数结构

```python
def simulate_typing(page: Page, selector: str, text: str, delay_range: Tuple[float, float]) -> None:
    """模拟人类打字输入行为。

    Args:
        page (Page): 当前 Playwright 页面对象
        selector (str): 要填入的输入框选择器
        text (str): 要输入的文字内容
        delay_range (Tuple[float, float]): 每个字符之间的延迟范围（秒）
    """
    for char in text:
        page.fill(selector, char)
        time.sleep(random.uniform(*delay_range))
```
## attached

### 📌 `src/session_manager.py`
- 负责管理登录流程与 session 持久化，支持复用 `storage_state.json`
- Commands:
  - 避免重复登录
  - 支持首次手动登录 + 会话保存
  - 后续自动加载已保存的状态

---

### 📌 `src/receipt_printer.py`
- 监听打印页面的弹窗，确保 PDF 保存格式、命名与路径规范
- Commands:
  - 使用 `context.on("page")` 捕捉新窗口
  - 使用 `page.pdf()` 进行 PDF 保存
  - 异常截图保存至 `logs/errorshots/`

---

### 📌 `src/humanizer.py`
- 模拟人类操作行为，降低被封禁风险
- Commands:
  - 使用 `time.sleep` + `random.uniform` 实现等待
  - 封装打字延迟、滚动行为、鼠标模拟等

---

### 📌 `src/excel_reader.py`
- 读取 Excel 文件所有 sheet 的 F 列作为提单号列表
- Commands:
  - 支持 sheet 全部遍历或指定某个 sheet
  - 列名应可配置，默认读取 `F` 列

---

### 📌 `src/main.py`
- 作为主执行入口，调度所有子模块、捕获异常、输出日志
- Commands:
  - 每个订单号处理需单独包裹 `try/except`
  - 所有任务完成后输出处理报告

---



